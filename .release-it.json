{
  "git": {
    "commitMessage": "Craft v${version} release",
    "requireCommits": true
  },
  "github": {
    "release": true,
    "draft": true,
    "assets": ["dist/**/*.zip", "dist/**/*.user.js", "dist/**/*.meta.js"],
    "releaseNotes": "npx auto-changelog --stdout -u -l false -t compact --hide-empty-releases --hide-credit"
  },
  "npm": {
    "publish": false
  },
  "hooks": {
    "after:bump": [
      "npm run build",
      "npx auto-changelog -p -u -l false -t keepachangelog --hide-empty-releases --hide-credit"
    ]
  }
}